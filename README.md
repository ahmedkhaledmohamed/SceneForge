# GenerationStyled

Tools for the GenerationStyled content creation workflow.

- **SceneForge** — local-first AI video production CLI (image generation → image-to-video → stitch). See the `sceneforge-mvp` branch / PR for the initial implementation.
